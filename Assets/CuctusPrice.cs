using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class CuctusPrice : MonoBehaviour
{
    [SerializeField] GameObject defenderPrefab;
    void Start()
    {
        TextMeshProUGUI costText = GetComponent<TextMeshProUGUI>();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
